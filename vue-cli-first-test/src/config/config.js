/**
 * 配置编译环境和线上环境之间的切换
 *
 * baseUrl: 域名地址----静态网页的路径--接口请求路径
 * routerMode: 路由模式
 * baseImgPath: 图片存放地址
 * // 基本配置
 */
let baseUrl = '';
let routerMode = 'history';

export {
  baseUrl,
  routerMode
};
